import { useEffect, useState } from 'react';
import { io } from 'socket.io-client'; const socket = io('http://localhost:5000'); function App() { const [connected, setConnected] = useState(false); useEffect(() => { socket.on('connect', () => { console.log('Connected to server with id:', socket.id); setConnected(true); }); socket.on('disconnect', () => { setConnected(false); }); }, []); return (<div style={{ padding: '2rem', fontFamily: 'sans-serif' }}> <h1>CampusConnect</h1> <p>Socket status: {connected ? '🟢 Connected' : '🔴 Not connected'}</p> </div>); } export default App; 